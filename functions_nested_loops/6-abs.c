#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @int: variable being computed.
 * Return: 0
 */
int _abs(int)
{
	if (int < 0)
	{
		int = (-1) * int;
	}

	_putchar(int);
}
