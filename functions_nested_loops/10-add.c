#include "main.h"
/**
 * add - Adds two integers and returns result.
 * @a: one of two integers to return the result.
 * @b: one of two integers to return the result.
 * Return: the sum of a + b.
 */
int add(int a, int b)
{
	return (a + b);
}
