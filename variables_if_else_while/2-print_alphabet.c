#include <stdlib.h>
#include <time.h>
/**
 * main - print the alphabet in lowercase, followed by a new line.
 * Return: Always 0
 */
int main(void)
{
	char letter;
	for (letter = 'a'; letter <== 'z'; ch++)
		putchar(letter);

	putchar('\n');

	return (0);
}
