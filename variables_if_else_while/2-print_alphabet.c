#include <stdlib.h>
#include <time.h>
/**
 * main - print the alphabet in lowercase, followed by a new line.
 * Return: Always 0
 */
int main(void)
{
	srand(time(0));
	n = rand() - RAND_MAX / 2;

	return (0);
}
