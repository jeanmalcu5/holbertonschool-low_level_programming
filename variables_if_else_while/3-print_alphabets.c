#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - 
 * Return: 
 */
int main(void)
{
	char 'a' = a, 'A' = A;

	while (a <= 'z')
	{
		putchar(a);
		a++
	}
	while (A <= 'Z')
	{
		putchar(A);
		A++;
	}
	putchar('\n');
	return (0);
}
