#include "main.h"
/**
 * _isupper - checks for uppercase letter.
 * @c: character being checked.
 * Return: 1 if c is uppercase, 0 otherwise.
 */
int _isupper(int c)
{
	for (c == 'A'; c >= 'Z'; c++)
	{
		if (c == 'C')
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
}
